# ============================================================================
# Core FastAPI & ASGI
# ============================================================================
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.6.0

# ============================================================================
# LangChain & LangGraph (Orchestration)
# ============================================================================
langchain==0.3.7
langchain-openai==0.2.8
langchain-anthropic==0.2.4
langchain-groq==0.2.1
langchain-community==0.3.7
langgraph==0.2.45
langgraph-checkpoint-postgres==2.0.6  # Critical: Postgres checkpointer

# ============================================================================
# Database (Postgres + Supabase)
# ============================================================================
asyncpg==0.30.0  # Async Postgres driver for checkpointer
supabase==2.9.1
postgrest==0.17.2
psycopg2-binary==2.9.10  # For synchronous operations if needed

# ============================================================================
# Vector Database (pgvector)
# ============================================================================
pgvector==0.3.5

# ============================================================================
# Redis (Cache & Pub/Sub)
# ============================================================================
redis==5.2.0
hiredis==3.0.0  # C parser for performance

# ============================================================================
# Privacy & PII Detection
# ============================================================================
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355
spacy==3.8.2  # Required for Presidio NER

# ============================================================================
# Embeddings & Semantic Search
# ============================================================================
sentence-transformers==3.3.1  # Local embeddings (HuggingFace)
faiss-cpu==1.9.0.post1  # Fast similarity search (CPU version)

# ============================================================================
# Utilities & Helpers
# ============================================================================
python-dotenv==1.0.1
httpx==0.27.2  # Async HTTP client
tenacity==9.0.0  # Retry logic
python-jose[cryptography]==3.3.0  # JWT handling
passlib[bcrypt]==1.7.4  # Password hashing
tiktoken==0.8.0  # Token counting for OpenAI

# ============================================================================
# Monitoring & Logging
# ============================================================================
structlog==24.4.0  # Structured logging
prometheus-client==0.21.0  # Metrics export

# ============================================================================
# Development Tools (optional, comment out for production)
# ============================================================================
pytest==8.3.4
pytest-asyncio==0.24.0
black==24.10.0
ruff==0.8.2
mypy==1.13.0

# ============================================================================
# Additional Language Models (Optional, for Turkish NLP)
# ============================================================================
# transformers==4.46.3  # If you need custom Turkish models
# torch==2.5.1  # Required for some HuggingFace models (CPU version)
